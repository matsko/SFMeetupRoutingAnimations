import { SwapDirective } from './swap.directive';

describe('SwapDirective', () => {
  it('should create an instance', () => {
    const directive = new SwapDirective();
    expect(directive).toBeTruthy();
  });
});
